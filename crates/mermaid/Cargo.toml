[package]
name = "mermaid"
version = "0.4.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Mermaid fence rendering (text-grid) for stele, with fallback to a plain code fence"

[lib]
name = "mermaid"
path = "src/lib.rs"

[dependencies]
mermaid-text = "=0.57.0"
